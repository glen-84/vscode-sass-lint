{
    "version": "0.1.0",
    // List of configurations. Add new configurations or edit existing ones.
    "configurations": [
        {
            "name": "Attach",
            "type": "node",
            "request": "attach",
            "address": "localhost",
            "protocol": "inspector",
            "port": 6010,
            "sourceMaps": true,
            "outFiles": ["${workspaceRoot}/../sass-lint/server/**/*.js"]
        }
    ]
}
